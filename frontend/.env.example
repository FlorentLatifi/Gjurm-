# Public URL the site is served from (canonical links, Open Graph, sitemap). No trailing slash.
VITE_PUBLIC_URL=https://gjurme.example.com
# API base. Empty = same origin (Caddy serves the SPA and proxies /api).
VITE_API_BASE=
